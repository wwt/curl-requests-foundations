#!/bin/sh
# Step 3 - Store the XSRF token in an environment variable
# Copy the XSRF token from the response and paste it into the following command, in place of the _______ characters
# echo export XSRF_TOKEN=_______ >> 03_sdwan_set_token.sh
